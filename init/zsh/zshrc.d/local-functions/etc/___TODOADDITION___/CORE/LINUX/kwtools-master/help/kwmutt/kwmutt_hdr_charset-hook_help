# kwmutt_hdr_charset-hook_help fuer kwmutt{{{
TITLE="${lv_Charset}::${gv_Help}"
MSG=$(gettext 'Mit dem Befehl charset-hook kann man Alias-Namen fuer Zeichensaetze
definieren. Dies ist nuetzlich, um Nachrichten richtig anzuzeigen,
in denen ein Zeichensatz deklariert wurde, den mutt nur unter
anderem Namen kennt.

Beispiel (chaset-hook Alias Zeichensatz): 
charset-hook ^gb2312$ gb18030

Alias: Geben Sie einen Alias Namen fuer den Zeichensatz ein den
\tMutt vom Namen her kennt.

Zeichensatz: Geben Sie den Zeichensatz ein fuer den der Alias
\tgelten soll.
')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}
